        val name = arrayOf("Tariq")

        val a:ArrayAdapter<String> = ArrayAdapter(this, android.R.layout.simple_expandable_list_item_1, name)

        listView.adapter=a
        
        listView.setOnItemClickListener { adapterView, view, i, id -> 
            
            val n = adapterView.getItemAtPosition(i).toString()
            Toast.makeText(this,n,Toast.LENGTH_SHORT).show()
        }
